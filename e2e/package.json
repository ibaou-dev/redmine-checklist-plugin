{
  "name": "redmine-checklist-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end browser tests for the redmine_checklist plugin (Playwright + Chrome).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
